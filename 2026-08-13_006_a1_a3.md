# Run 2026-08-13_006_a1_a3

## Purpose

Implement the v3.4 A1 frontend/tensor contract and audit the second backbone A3
without launching any Gate, admission probe, or paper-level experiment.

## Cold-start and state reconciliation

- Read `AI_START_HERE.md`, `PROJECT_STATE.yaml`, `HANDOFF.md`, `TASKS.yaml`, and both guide files in the mandated order.
- Ran `check_project_state.py` and `project_status.py` before implementation; the initial state was valid with 25 tasks and 2 completed tasks.
- Reconciled the stale `B_ZUCO2_NOT_EXTRACTED` claim using a deterministic archive/extraction comparison: 1350 regular files, 128920389085 uncompressed bytes, exact relative-path set match, and zero bad CRC entries. Shallow MATLAB/HDF5 schema and channel observations are recorded in `artifacts/zuco2_extraction_manifest.yaml`.
- No VCS metadata exists under `/home/song/projects`; existing files were preserved.

## A1 implementation

- Added `02_code/src/backbones/a1_spectral.py` and package export.
- Added deterministic eight-band bandpower extraction, ET word-level and ET-free fixed-window APIs, fold-local median/IQR normalization with 0.5/99.5 clipping, explicit padding mask, and a small shared Transformer alignment encoder.
- Added `A1ChannelMap`; 128-channel continuous input is rejected unless a caller supplies an explicit verified map to the 105-channel contract. The synthetic test map is labeled non-ZuCo and is not an experimental mapping.
- Added `02_code/tests/test_a1_contract.py` and `02_code/scripts/a1_contract_selfcheck.py`.

## A1 self-check summary

- Seed: `20260813`; fold: `A1-contract-1`; method: `A1`.
- Unit tests: `7/7 PASS`.
- Synthetic sequences: word `[4,840]`, fixed `[7,840]`; padded `[2,7,840]`; bool mask `[2,7]`; encoder output `[2,256]`.
- Feature range: `6.72e-05..6.78e-02`; normalized absolute max `3.7922`; output absolute max `1.6254`; runtime `0.451 s`.
- Config hash: `163c8d99624c95d15fd68d76e6ce8d7d30f2f140021c807bcef7aa1d62a2eb97`.
- Assertions: feature dimensions, finite values, bool mask, output shape, and <=20M parameters all PASS.

## A3 audit summary

- Architecture and pooled dimension 200 are supported by official source code/paper.
- Corpus contamination exclusion is not proven because the public corpus list contains an opaque self-collected EEG block; CO-N7 remains open.
- Checkpoint identity/rights/hash, exact model channel subset/order, ZuCo mapping, and extractor runtime are unresolved. A3 remains BLOCKED and is not in T6.

## Remaining blockers and boundary

- A1 real-data admission is blocked by the missing verified 128-to-105 channel map and incomplete bandpower formula/unit freeze.
- ZuCo data card, semantic item, split, H, leakage audit, Gate A population, and all gates remain untouched.
- No held-out result, threshold, seed, fold, candidate list, or null was changed.

## End-of-session self-check

After deploying the state files, `check_project_state.py` reported
`PROJECT STATE VALID | tasks=25 | done=4`; `project_status.py` recommended
`S0_DATA_CARD` with the route unlocked and A3 still blocked. The A1-specific suite
reported `7/7 PASS`, and the full remote suite reported `16/16 PASS`.
