# Run 2026-08-12_005

## Purpose

Register the completed TMNRED download and perform a deterministic BIDS metadata
inventory without changing experiment hyperparameters or launching training.

## Inputs

- OpenNeuro `ds005383`, snapshot `1.0.0`.
- Local target: `01_data_protocol/datasets/tmnred_ds005383_v1.0.0`.
- Download report and S3 manifest under `01_data_protocol/datasets/_downloads/`.

## Actions

- Marked `S0_TMNRED_AVAILABILITY` as DONE because the source, license metadata,
  local path, and integrity evidence are now recorded.
- Added the availability record and TMNRED data card.
- Added a deterministic metadata audit script and generated the assignment matrix,
  support inventory, and audit JSON.
- Kept `S0_TMNRED_DATA_CARD` BLOCKED because leakage audits are pending and the
  `participants.tsv` header/row alignment requires manual review.
- Updated the project blocker from the obsolete "TMNRED unavailable" statement to
  the remaining data-card audit blocker.

## Self-Check Summary

- Subjects: `30`.
- Sessions: `8` per subject (`240` event files).
- Event rows: `11991`; rows per event file: `41..50`.
- Stimulus IDs: `50` (`15..64`); each stimulus is covered by `30` subjects;
  observed event counts are `239..240`.
- Materials: `50` rows.
- EEG: continuous, `200 Hz`, `30` EEG channels plus one trigger row in the channel table.
- Randomness: none; no seed required.
- Audit runtime: `0.185 s` (metadata audit only).

## Validation Boundary

No model, split, probe, threshold, or gate was run. TMNRED is available and its
assignment inventory is balanced, but it is not yet cleared for a complete CSPE or
EQ-ANMA experiment.
